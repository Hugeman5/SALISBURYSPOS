{
  "name": "salisburys-functions",
  "private": true,
  "engines": { "node": ">=18" },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -w -p tsconfig.json"
  },
  "dependencies": {
    "firebase-admin": "^12",
    "firebase-functions": "^5"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20"
  }
}